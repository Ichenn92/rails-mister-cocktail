<%= simple_form_for @cocktail, html: { class: "form-inline" } do |f| %>
  <%= f.input :name, placeholder: "Name of your fancy cocktail", label: false %>
  <%= f.submit "Add", class: "btn btn-primary" %>
<% end %>
